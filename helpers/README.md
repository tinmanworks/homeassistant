# Helpers

Domain-specific helper entities.

- `input_boolean/`
- `input_number/`
- `input_select/`
- `input_datetime/`

If a helper already exists as a UI-managed entity in Home Assistant, disable or remove the UI version before enabling the YAML version with the same entity ID.
