# Automations

Production automations.

Suggested placement:
- `core/` for global/home-wide automations
- `rooms/` for room-specific automations
- `system/` for maintenance/ops automations

If an automation already exists as a UI-managed automation in Home Assistant, disable or remove the UI version before enabling the YAML version with the same entity ID.
