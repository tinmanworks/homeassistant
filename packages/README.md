# Packages

Composable Home Assistant packages.

Suggested placement:
- `rooms/` for room-level package bundles
- `system/` for operational package bundles
- `integrations/` for integration-focused bundles
