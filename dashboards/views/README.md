# Views Dashboards

Cross-room dashboards for day-to-day usage.

`all_entities.yaml` is generated/updated by:

```bash
python3 python/tools/dashboard_export.py sync-live --ha-url "$HA_URL" --ha-token "$HA_TOKEN"
```
